package com.idle.togeduck.quest.exchange.view.my_exchange_rv

import com.idle.togeduck.quest.exchange.model.Exchange

interface IMyExchangeDetail {
    fun myExchangeItemClicked(myExchange: Exchange)
}